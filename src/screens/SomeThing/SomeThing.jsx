import { Text, View } from 'react-native';

export const SomeThing = () => {
   return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
         <Text>SomeThing.JSX</Text>
      </View>
   );
};
