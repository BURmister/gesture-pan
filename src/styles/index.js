import * as Colors from './colors';
import * as Typography from './typography';

const _colors = Colors.getColors; // вынули функцию получения цветов для текущей темы

export { Colors, _colors, Typography };
